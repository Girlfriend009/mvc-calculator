public class CalculatorApp {
    public static void main(String[] args) {

        // Создаем экземпляры модели, представления и контроллера
        CalculatorView view = new CalculatorView();     // Представление

        // Делаем окно видимым
        view.setVisible(true);
    }
}
